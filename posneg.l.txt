%{
#include <stdio.h>
int pos = 0, neg = 0;
%}

%%
-[0-9]+   { neg++; }
[0-9]+    { pos++; }
%%
int yywrap() { return 1; }

int main()
{
    yylex();
    printf("Positive numbers = %d\n", pos);
    printf("Negative numbers = %d\n", neg);
    return 0;
}
