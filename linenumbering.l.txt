%{
#include <stdio.h>
int ln = 0;
%}

%%

.*           { ln++; fprintf(yyout, "\n%d: %s", ln, yytext); }

%%

int yywrap() { return 1; }

int main() {
    yyin = fopen("simple1.txt", "r");
    yyout = fopen("outnew.txt", "w");
    yylex();
    fclose(yyin);
    fclose(yyout);
    return 0;
}