%{
#include <stdio.h>
%}

%%
"="|"+"|"-"|"/"|"*"    { printf("\nValid operator: %s", yytext); }
[0-9]+                 { printf("\nValid number: %s", yytext); }
[ \t\n]                { /* ignore whitespace */ }
.                      { printf("\nInvalid token: %s", yytext); }
%%

int yywrap(void) { return 1; }

int main() {
    printf("Enter the input: ");
    yylex();
    printf("\n");
    return 0;
}