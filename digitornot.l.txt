%{
#include <stdio.h>
%}

%%
[0-9]+                  { printf("\n%s is DIGIT", yytext); }          /* integer */
[0-9]*\.[0-9]+          { printf("\n%s is DIGIT", yytext); }          /* float */
[ \t\n]                 { /* ignore whitespace */ }
.+                      { printf("\n%s is NOT A DIGIT", yytext); }    /* anything else */
%%

int yywrap(void) { return 1; }

int main() {
    printf("Enter the input: ");
    yylex();
    printf("\n");
    return 0;
}